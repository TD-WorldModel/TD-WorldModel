_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
vendor/

# LaTeX source archives — kept out of the site repo
*.zip
# Stray figure copy in repo root (the canonical one lives in static/images/)
/patch_pc1_clean.png
